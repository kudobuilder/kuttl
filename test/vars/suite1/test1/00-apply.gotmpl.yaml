apiVersion: v1
kind: Pod
metadata:
  # The b64* invocations are making sure that sprig works.
  name: foo-{{ .Namespace }}-var1-{{ .Vars.var2 }}-{{ "var3" | b64enc | b64dec }}
spec:
  containers:
    - name: foo
      image: foo